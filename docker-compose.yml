version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: dealse-postgres
    restart: always
    environment:
      POSTGRES_USER: dealse_user
      POSTGRES_PASSWORD: dealse_password
      POSTGRES_DB: dealse_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    container_name: dealse-redis
    restart: always
    ports:
      - "6379:6379"

volumes:
  postgres_data:
